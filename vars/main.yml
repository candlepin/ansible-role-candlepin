# Variables used by various tasks in the role. These should only be updated with respect to changes
# in the Candlepin repo itself, or the tasks surrounding its setup.
---
cp_deploy_script: "{{candlepin_home}}/bin/deployment/deploy"
cp_vagrant_mapping: "/vagrant"

cp_yourkit_guest_home: "/opt/yjp/bin/linux-x86-64"
cp_yourkit_guest_library: "{{cp_yourkit_guest_home}}/libyjpagent.so"
