---
dependency:
  name: galaxy
driver:
  name: docker
  # name: podman
platforms:
  - name: instance
    image: "geerlingguy/docker-${MOLECULE_DISTRO:-fedora35}-ansible:latest"
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
# lint: |
#   set -e
#   yamllint .
#   ansible-lint .
